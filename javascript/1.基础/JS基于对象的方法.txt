JS是一种基于对象的语言
	1.具备对象的基本结构
		由属性和方法两个基本元素构成。
	2.引用对象的途径
		引用js内置对象，由浏览器内部提供，可以创建新的对象。
	3.有关对象操作的语句。
		1)for..in语句遍历
		2)obj[item]调用
		3)obj.item调用
java面向对象特点：
	面向对象的基本思想是使用类，对象，继承，封装，消息等概念进行程序设计。
	1.封装
	2.继承
	3.多态